# CODSOFT - Movie Genre Classifier

Run locally:
1. Create and activate venv
2. pip install -r requirements.txt
3. python -c "import nltk; nltk.download('stopwords'); nltk.download('wordnet')"
4. streamlit run app.py